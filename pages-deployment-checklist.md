# قائمة تحقق Cloudflare Pages المستقبلية — لا نشر

قبل أي نشر مستقبلي، يجب أن يكون القرار مكتوباً وصريحاً. لا تنفذ أي بند أدناه ضمن Migration Lab الحالي.

| التحقق | الحالة المطلوبة قبل التنفيذ |
|---|---|
| اعتماد فصل Development عن Production | موافقة صريحة من مالك المشروع. |
| مراجعة Supabase Auth وروابط redirect | حسابات اختبار فقط ثم مراجعة مستقلة. |
| إغلاق صفحات MySQL المتبقية أو توثيق بقائها | Dashboard، الأرشيف، التصدير، الطباعة، PDF، QR، المستخدمون، Offline Queue. |
| مراجعة RLS وRPC | اختبار إداري ومستخدم عادي وسجل تدقيق وIdempotency. |
| إعداد متغيرات البيئة | عبر قناة الأسرار فقط؛ لا توضع في `wrangler.toml`. |
| ربط النطاق وDNS | موافقة مستقلة لاحقة؛ غير مسموح الآن. |
| نشر Pages/Workers | موافقة مستقلة لاحقة؛ غير مسموح الآن. |
