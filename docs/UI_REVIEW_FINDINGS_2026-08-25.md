# نتائج فحص الواجهة — 25 أغسطس 2026

تمت مراجعة مسارات `/`, `/licenses`, `/archive`, `/licenses/new`, `/trash`, `/team`, و`/migration-lab/supabase` على سطح المكتب 1280×720 والجوال 375×812.

- اتجاه RTL واللغة العربية ظاهران في المسارات الأساسية.
- لوحة التحكم وسجل التراخيص والأرشيف ونموذج الإضافة والسلة والفريق ومختبر Supabase ظهرت دون أخطاء تصيير مرئية.
- التخطيط الجوال يحول الجداول إلى بطاقات ويكدس الفلاتر والأزرار، مع بقاء أزرار البحث والإضافة قابلة للرؤية.
- نموذج الإضافة يعرض رقم الترخيص كحقل إدخال، ويعرض بيانات الأرشفة كتعبئة تلقائية غير قابلة للتحرير.
- السلة الفارغة تعرض حالة فراغ واضحة، ومختبر Supabase يعرض نموذج دخول Development فقط.
- ظهرت بيانات المختبر/قاعدة الاختبار الحالية في اللقطات؛ لم تُرسل نماذج ولم تُنشأ بيانات أثناء اللقطات.
- فحص بصري لا يثبت وحده كل تفاعلات البحث والفلاتر أو الطباعة؛ يلزم الاعتماد على الاختبارات الوحدوية/التكاملية مع إبقاء اختبار القبول اليدوي موثقاً كمرحلة لاحقة.
- سجل الخادم كان يعمل، وظهر تحذير أداة `baseline-browser-mapping` قديمة فقط، وليس خطأ تطبيق.

## مراجعة شريحة القراءة من Supabase Development

تمت مراجعة صفحة `/licenses` ومسار التجربة المحمي `/licenses?backend=supabase-development` على سطح المكتب. ظل المسار الافتراضي يعرض قائمة MySQL/tRPC كما كان. ويظهر وضع Supabase النص التوضيحي نفسه مع رسالة آمنة تطلب جلسة حساب اختبار Supabase معتمد قبل جلب أي بيانات؛ لذلك لا يمكن تفعيل مصدر Development بالخطأ ولا يفتح تفاصيل السجلات أو الإجراءات الإدارية قبل اكتمال نقلها. لم تدخل بيانات أو تُرسل نماذج أثناء هذا التحقق.

## مراجعة الإضافة والتفاصيل الانتقالية

تمت مراجعة `/licenses/new?backend=supabase-development` و`/supabase-development/licenses/:id` على سطح المكتب. بقي نموذج الإدخال العربي وحقول رقم الترخيص اليدوي ومعاينة رقم الأرشفة كما هي، مع وسم واضح لمسار Development يشرح أن المرفقات وطابور Offline لم ينتقلا إليه. كما تعيد صفحة التفاصيل التجريبية غير الموثقة إلى صفحة دخول المختبر بدلاً من محاولة عرض أي سجل. لم تُنشأ أو تُعدل بيانات خلال المراجعة البصرية.

## مراجعة Dashboard والأرشيف المستقلين في Supabase Development

تم التقاط معاينتين لمساري `/supabase-development/dashboard` و`/supabase-development/archive` على سطح المكتب بعرض 1280 بكسل. حافظ المساران على اتجاه RTL، وأظهرا حالة اتصال وطلب تسجيل الدخول بحساب Supabase Development اصطناعي قبل عرض البيانات. لم تُرسل نماذج ولم تُنشأ بيانات أثناء الفحص. تظهر حالة الحماية المقصودة بدلاً من شاشة فارغة، ويوجد زر واضح للعودة إلى مختبر Supabase. لم يُختبر العرض المصادق عليه تفاعلياً في هذه الجولة لأن ذلك يتطلب جلسة اختبار؛ وتبقى هذه الملاحظة ضمن اختبار القبول قبل أي نشر.

## مراجعة المسارات المستقلة الجديدة — سطح المكتب

تمت معاينة `/supabase-development/dashboard` و`/supabase-development/archive` و`/supabase-development/team` و`/supabase-development/licenses/test/print` بعرض 1280 بكسل دون جلسة مصادقة. ظهرت في جميعها شاشة الحماية العربية التي تطلب حساب Supabase Development اصطناعياً قبل تحميل البيانات، مع RTL وزر رجوع واضح إلى المختبر. لم تُعرض بيانات أو تُنفذ عمليات، وتبقى المعاينة المصادق عليها للطباعة وإدارة الفريق ضمن اختبار القبول الآمن لاحقاً.

## مراجعة المسارات المستقلة الجديدة — الجوال

تمت معاينة المسارات الأربعة نفسها بعرض 375 بكسل. بقي RTL واضحاً، وتكدست رسالة الحماية وزر المختبر بصورة قابلة للقراءة دون خروج أفقي ظاهر في Dashboard والأرشيف والفريق. ظهرت صفحة الطباعة أيضاً ضمن حالة طلب جلسة Supabase قبل جلب سجل الاختبار، وهو سلوك آمن يمنع العرض غير المصرح به. لم تُستخدم جلسة حقيقية أو بيانات Production.
