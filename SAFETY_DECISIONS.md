# قرارات الأرشفة والحذف

يحوّل إجراء الأرشفة حالة الترخيص إلى `archived` ويطلب سبباً إلزامياً، مع الاحتفاظ بكامل بيانات الترخيص ووثائقه وسجل أحداثه. يقتصر هذا الإجراء على مدير النظام ويُسجل في سجل التدقيق.

يتطلب الحذف النهائي كتابة رقم الترخيص نفسه في مربع التأكيد بعد فتح نافذة تحذير مستقلة. يزيل هذا الإجراء سجل الترخيص وبيانات المستندات والتنبيهات والأحداث من قاعدة البيانات، بينما يحتفظ بسجل تدقيق يبيّن من قام بالحذف وبياناته الأساسية. مفاتيح الملفات الخاصة تزول مع سجلات المستندات، فلا تبقى لها مراجع داخل النظام.
